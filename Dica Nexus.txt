1. Camada Sensorial (Visual & Áudio)Identidade Visual & Design TokensCSS/* Tokens Elétricos de Alta Fidelidade */
--reactor-cyan: #00f5ff;
--reactor-cyan-glow: rgba(0, 245, 255, 0.45);
--reactor-amber: #ffb800;
--reactor-amber-glow: rgba(255, 184, 0, 0.4);
--reactor-breaker-red: #ff2a55;
--reactor-breaker-glow: rgba(255, 42, 85, 0.5);
--reactor-mesh-bg: #090d14;
--reactor-plate: rgba(15, 23, 42, 0.85);
Prompt para o Gemini Image Engine (Hero Asset / Background Tático)Cinematic sci-fi high-voltage tactical laboratory terminal, glowing cyan plasma conduit tubes transferring raw energy along an intricate logic busbar, modular retro-futuristic circuit breakers, analog needle voltage gauges showing 100 KV load, dark brushed-titanium server racks, volumetric blue and amber rim lighting, shallow depth of field, Raytraced Unreal Engine 5 render, ultra-detailed 8k, isometric perspective, clean product design.Síntese Sonora Zero-Dependency (src/lib/audio.ts)TypeScriptlet sharedAudioCtx: AudioContext | null = null;

function getAudioContext(): AudioContext | null {
  if (typeof window === 'undefined') return null;
  if (!sharedAudioCtx) {
    const AudioCtx = window.AudioContext || (window as unknown as { webkitAudioContext: typeof AudioContext }).webkitAudioContext;
    if (AudioCtx) sharedAudioCtx = new AudioCtx();
  }
  if (sharedAudioCtx && sharedAudioCtx.state === 'suspended') {
    sharedAudioCtx.resume().catch(() => {});
  }
  return sharedAudioCtx;
}

/** Fechamento de contato elétrico e pulso capacitivo ascendente */
export function playCircuitConnectSound(enabled = true) {
  if (!enabled) return;
  const ctx = getAudioContext();
  if (!ctx) return;

  const now = ctx.currentTime;
  const osc = ctx.createOscillator();
  const gain = ctx.createGain();
  const filter = ctx.createBiquadFilter();

  osc.type = 'triangle';
  osc.frequency.setValueAtTime(320, now);
  osc.frequency.exponentialRampToValueAtTime(940, now + 0.14);

  filter.type = 'lowpass';
  filter.frequency.setValueAtTime(1200, now);
  filter.frequency.exponentialRampToValueAtTime(3600, now + 0.14);

  gain.gain.setValueAtTime(0.001, now);
  gain.gain.linearRampToValueAtTime(0.18, now + 0.02);
  gain.gain.exponentialRampToValueAtTime(0.0001, now + 0.16);

  osc.connect(filter);
  filter.connect(gain);
  gain.connect(ctx.destination);

  osc.start(now);
  osc.stop(now + 0.16);
}

/** Faísca de arco voltaico com desarme violento de disjuntor */
export function playCircuitShortSound(enabled = true) {
  if (!enabled) return;
  const ctx = getAudioContext();
  if (!ctx) return;

  const now = ctx.currentTime;

  // Ruído de faísca (arco elétrico)
  const bufferSize = ctx.sampleRate * 0.08;
  const buffer = ctx.createBuffer(1, bufferSize, ctx.sampleRate);
  const data = buffer.getChannelData(0);
  for (let i = 0; i < bufferSize; i++) {
    data[i] = (Math.random() * 2 - 1) * Math.exp(-i / (bufferSize * 0.3));
  }
  const noise = ctx.createBufferSource();
  noise.buffer = buffer;
  const noiseGain = ctx.createGain();
  noiseGain.gain.setValueAtTime(0.25, now);
  noiseGain.gain.exponentialRampToValueAtTime(0.001, now + 0.08);
  noise.connect(noiseGain);
  noiseGain.connect(ctx.destination);
  noise.start(now);

  // Queda mecânica do disjuntor (onda dente de serra descendente pesada)
  const osc = ctx.createOscillator();
  const gain = ctx.createGain();
  osc.type = 'sawtooth';
  osc.frequency.setValueAtTime(140, now);
  osc.frequency.exponentialRampToValueAtTime(38, now + 0.22);

  gain.gain.setValueAtTime(0.2, now);
  gain.gain.exponentialRampToValueAtTime(0.001, now + 0.22);

  osc.connect(gain);
  gain.connect(ctx.destination);
  osc.start(now);
  osc.stop(now + 0.22);
}

/** Turbina de ressonância harmônica em sobrecarga triunfal de 100% */
export function playReactorOverdriveSound(enabled = true) {
  if (!enabled) return;
  const ctx = getAudioContext();
  if (!ctx) return;

  const now = ctx.currentTime;
  const frequencies = [261.63, 329.63, 392.0, 523.25, 659.25]; // C E G C E

  frequencies.forEach((freq, idx) => {
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();
    const delay = idx * 0.07;

    osc.type = 'sine';
    osc.frequency.setValueAtTime(freq * 0.8, now + delay);
    osc.frequency.exponentialRampToValueAtTime(freq * 1.5, now + delay + 0.4);

    gain.gain.setValueAtTime(0.001, now + delay);
    gain.gain.linearRampToValueAtTime(0.12, now + delay + 0.05);
    gain.gain.exponentialRampToValueAtTime(0.0001, now + delay + 0.6);

    osc.connect(gain);
    gain.connect(ctx.destination);

    osc.start(now + delay);
    osc.stop(now + delay + 0.65);
  });
}
2. Motor Lógico Determinístico (src/lib/circuit-engine.ts)TypeScriptexport interface CircuitNode {
  id: string;
  stageIndex: number;
  terminalLabel: string;
  stageName: string;
  correctAnswer: string;
  options: string[];
}

export interface CircuitState {
  nodes: CircuitNode[];
  currentNodeIndex: number;
  conductionPercent: number;
  isShortCircuited: boolean;
  isCompleted: boolean;
  selectedOption: string | null;
  shortCircuitReason: string | null;
}

const DISTRACTOR_TEMPLATES = [
  'Inversão de causa e efeito: reação isolada tomada como fundamento primário.',
  'Correlação espúria externa desprovida de nexo causal mecânico.',
  'Estagnação estática do sistema sem dissipação ou transferência de carga.',
  'Gradiente inverso: atenuação de fluxo onde há indução exponencial.'
];

export function buildCircuitNodes(cardData: any): CircuitNode[] {
  const qualitativeChain = [
    { label: 'TERMINAL 01: POLO GERADOR', name: 'Causas Materiais & Premissas', data: cardData?.causalChain?.causes },
    { label: 'TERMINAL 02: CONDUTOR ATIVO', name: 'Agentes Catalisadores', data: cardData?.causalChain?.agents },
    { label: 'TERMINAL 03: REATOR CENTRAL', name: 'Mecanismos de Ação', data: cardData?.causalChain?.mechanisms },
    { label: 'TERMINAL 04: SAÍDA DE CARGA', name: 'Consequências Diretas', data: cardData?.causalChain?.consequences },
    { label: 'TERMINAL 05: REDE DISTRIBUÍDA', name: 'Desdobramentos e Impacto', data: cardData?.causalChain?.unfoldings }
  ];

  // Caso contenha proporcionalidade física/quantitativa
  if (cardData?.proportionality && Array.isArray(cardData.proportionality) && cardData.proportionality.length > 0) {
    return cardData.proportionality.slice(0, 5).map((prop: any, idx: number) => {
      const correct = `${prop.variable}: ${prop.effect || prop.relation || 'Relação Direta'}`;
      const inverted = `${prop.variable}: Inversamente proporcional com atenuação de 1/2`;
      const constant = `${prop.variable}: Permanece invariante independente da carga`;
      return {
        id: `prop-node-${idx + 1}`,
        stageIndex: idx,
        terminalLabel: `TERMINAL 0${idx + 1}: CALIBRAÇÃO`,
        stageName: prop.variable || `Variável de Estado 0${idx + 1}`,
        correctAnswer: correct,
        options: shuffleArray([correct, inverted, constant])
      };
    });
  }

  // Montagem qualitativa determinística
  return qualitativeChain.map((stage, idx) => {
    const rawVal = Array.isArray(stage.data) && stage.data.length > 0 
      ? stage.data[0] 
      : (typeof stage.data === 'string' ? stage.data : null);

    const correct = rawVal || `Pilar causal essencial da etapa ${stage.name}`;
    const distractor1 = DISTRACTOR_TEMPLATES[idx % DISTRACTOR_TEMPLATES.length];
    const distractor2 = `Neutralização prematura: supressão do efeito por dispersão inercial.`;

    return {
      id: `causal-node-${idx + 1}`,
      stageIndex: idx,
      terminalLabel: stage.label,
      stageName: stage.name,
      correctAnswer: correct,
      options: shuffleArray([correct, distractor1, distractor2])
    };
  });
}

function shuffleArray<T>(array: T[]): T[] {
  const arr = [...array];
  for (let i = arr.length - 1; i > 0; i--) {
    const j = Math.floor((i + 1) * 0.6180339887) % (i + 1); // Pseudo-aleatório estável
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr;
}
3. Estilos de Animação e Eletricidade (src/index.css)CSS/* Efeitos Dinâmicos do Reator Causal */
@keyframes circuitPulse {
  0% { stroke-dashoffset: 60; filter: drop-shadow(0 0 2px #00f5ff); }
  50% { stroke-dashoffset: 0; filter: drop-shadow(0 0 8px #00f5ff); }
  100% { stroke-dashoffset: -60; filter: drop-shadow(0 0 2px #00f5ff); }
}

@keyframes breakerSpark {
  0% { opacity: 1; transform: scale(1); filter: drop-shadow(0 0 12px #ff2a55); }
  50% { opacity: 0.4; transform: scale(1.02); filter: drop-shadow(0 0 24px #ff2a55); }
  100% { opacity: 1; transform: scale(1); filter: drop-shadow(0 0 12px #ff2a55); }
}

.cable-energized {
  stroke-dasharray: 8 6;
  animation: circuitPulse 1.2s linear infinite;
}

.short-circuit-active {
  animation: breakerSpark 0.25s ease-in-out infinite alternate;
}
4. Componente Reator Causal (src/components/study/StructureSection.tsx)TypeScriptimport React, { useState, useMemo } from 'react';
import { buildCircuitNodes, CircuitNode } from '../../lib/circuit-engine';
import { playCircuitConnectSound, playCircuitShortSound, playReactorOverdriveSound } from '../../lib/audio';

interface StructureSectionProps {
  card: any;
  soundEnabled?: boolean;
  onRewardXp?: (amount: number, label: string) => void;
}

export const StructureSection: React.FC<StructureSectionProps> = ({
  card,
  soundEnabled = true,
  onRewardXp
}) => {
  const [activeMode, setActiveMode] = useState<'canonical' | 'reactor'>('canonical');
  const [currentNodeIndex, setCurrentNodeIndex] = useState<number>(0);
  const [isShortCircuited, setIsShortCircuited] = useState<boolean>(false);
  const [completed, setCompleted] = useState<boolean>(false);
  const [lastError, setLastError] = useState<string | null>(null);

  const nodes = useMemo(() => buildCircuitNodes(card), [card]);
  const conductionPercent = Math.min(100, Math.round((currentNodeIndex / nodes.length) * 100));

  const handleConnect = (option: string, node: CircuitNode) => {
    if (isShortCircuited || completed) return;

    if (option === node.correctAnswer) {
      playCircuitConnectSound(soundEnabled);
      const nextIndex = currentNodeIndex + 1;
      setCurrentNodeIndex(nextIndex);

      if (nextIndex >= nodes.length) {
        setCompleted(true);
        playReactorOverdriveSound(soundEnabled);
        if (onRewardXp) {
          onRewardXp(150, '★ CIRCUITO CAUSAL ESTABILIZADO (100 KV) ★');
        }
      }
    } else {
      setIsShortCircuited(true);
      setLastError('Curto-circuito por inversão de causa-efeito detectado no terminal.');
      playCircuitShortSound(soundEnabled);
    }
  };

  const handleResetBreaker = () => {
    setIsShortCircuited(false);
    setLastError(null);
    playCircuitConnectSound(soundEnabled);
  };

  return (
    <section className="relative rounded-2xl bg-slate-950/80 border border-slate-800/80 p-6 backdrop-blur-xl overflow-hidden shadow-2xl">
      {/* Cabeçalho da Seção com Comutador Canônico / Reator */}
      <div className="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 mb-6 border-b border-slate-800 pb-4">
        <div>
          <span className="text-xs font-mono uppercase tracking-widest text-cyan-400">SEÇÃO 03 // MOTOR ESTRUTURAL</span>
          <h2 className="text-xl font-bold text-white tracking-tight flex items-center gap-2">
            Arquitetura Lógica da Matéria
            {completed && <span className="text-xs px-2 py-0.5 rounded bg-cyan-500/20 text-cyan-300 border border-cyan-500/30">CIRCUITO 100%</span>}
          </h2>
        </div>

        {/* Toggle Tático */}
        <div className="flex p-1 bg-slate-900 border border-slate-800 rounded-xl">
          <button
            onClick={() => setActiveMode('canonical')}
            className={`px-3 py-1.5 rounded-lg text-xs font-semibold tracking-wide transition-all ${
              activeMode === 'canonical'
                ? 'bg-slate-800 text-slate-100 shadow-sm'
                : 'text-slate-400 hover:text-slate-200'
            }`}
          >
            📋 Matriz Canônica
          </button>
          <button
            onClick={() => setActiveMode('reactor')}
            className={`px-3 py-1.5 rounded-lg text-xs font-semibold tracking-wide transition-all flex items-center gap-1.5 ${
              activeMode === 'reactor'
                ? 'bg-cyan-500/20 text-cyan-300 border border-cyan-500/40 shadow-[0_0_12px_rgba(0,245,255,0.2)]'
                : 'text-cyan-400/80 hover:text-cyan-300'
            }`}
          >
            ⚡ Reator Causal <span className="text-[10px] bg-cyan-400 text-slate-950 font-black px-1 rounded-sm">+150 XP</span>
          </button>
        </div>
      </div>

      {/* RENDERIZAÇÃO CANÔNICA (Preservação Total) */}
      {activeMode === 'canonical' && (
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          {nodes.map((n, i) => (
            <div key={n.id} className="p-4 rounded-xl bg-slate-900/60 border border-slate-800">
              <span className="text-[10px] font-mono text-slate-500 uppercase tracking-wider">{n.terminalLabel}</span>
              <h4 className="text-sm font-semibold text-slate-200 mt-1 mb-2">{n.stageName}</h4>
              <p className="text-xs text-slate-400 leading-relaxed">{n.correctAnswer}</p>
            </div>
          ))}
        </div>
      )}

      {/* RENDERIZAÇÃO DO REATOR DE ALTA TENSÃO */}
      {activeMode === 'reactor' && (
        <div className="space-y-6">
          {/* Painel de Voltagem & Status */}
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4 p-4 rounded-xl bg-slate-900/90 border border-cyan-500/30 shadow-[inset_0_0_20px_rgba(0,245,255,0.05)]">
            <div>
              <span className="text-[10px] font-mono text-cyan-400 uppercase">Status de Carga</span>
              <div className="text-2xl font-black font-mono text-cyan-300 mt-0.5">{conductionPercent} KV</div>
              <div className="w-full bg-slate-800 h-2 rounded-full mt-2 overflow-hidden border border-slate-700">
                <div
                  className="h-full bg-gradient-to-r from-cyan-500 to-emerald-400 transition-all duration-500 shadow-[0_0_10px_#00f5ff]"
                  style={{ width: `${conductionPercent}%` }}
                />
              </div>
            </div>

            <div className="flex flex-col justify-center">
              <span className="text-[10px] font-mono text-slate-400 uppercase">Estabilidade do Circuito</span>
              <div className="flex items-center gap-2 mt-1">
                <div className={`w-3 h-3 rounded-full ${isShortCircuited ? 'bg-red-500 animate-ping' : completed ? 'bg-emerald-400' : 'bg-cyan-400 animate-pulse'}`} />
                <span className={`text-xs font-mono font-bold ${isShortCircuited ? 'text-red-400' : completed ? 'text-emerald-300' : 'text-cyan-300'}`}>
                  {isShortCircuited ? 'DISJUNTOR DESARMADO' : completed ? 'CIRCUITO SUPERESTÁVEL' : 'ENERGIZANDO BANCADA'}
                </span>
              </div>
            </div>

            <div className="flex items-center justify-end">
              {isShortCircuited ? (
                <button
                  onClick={handleResetBreaker}
                  className="w-full md:w-auto px-4 py-2 rounded-lg bg-red-500/20 hover:bg-red-500/30 text-red-300 border border-red-500/50 text-xs font-mono font-bold tracking-wider transition-all"
                >
                  ⚡ REARMAR DISJUNTOR
                </button>
              ) : completed ? (
                <div className="text-xs font-mono text-emerald-400 font-bold border border-emerald-500/30 bg-emerald-500/10 px-3 py-1.5 rounded-lg">
                  ★ OVERDRIVE 100% CONCLUÍDO ★
                </div>
              ) : (
                <span className="text-xs font-mono text-slate-400">Terminal Ativo: #{currentNodeIndex + 1} de {nodes.length}</span>
              )}
            </div>
          </div>

          {/* Alerta de Curto-Circuito */}
          {isShortCircuited && (
            <div className="p-4 rounded-xl bg-red-950/40 border border-red-500/60 short-circuit-active flex items-center gap-3">
              <div className="p-2 rounded-lg bg-red-500/20 text-red-400 font-bold">⚠️</div>
              <div className="text-xs text-red-200 leading-relaxed font-mono">
                <strong className="text-red-400 block mb-0.5">DISPARO DE PROTEÇÃO TÉRMICA:</strong>
                {lastError} Conexão de polaridade incompatível com a ordem causal da matéria.
              </div>
            </div>
          )}

          {/* Diagrama de Terminais e Cabos */}
          <div className="grid grid-cols-1 sm:grid-cols-5 gap-3">
            {nodes.map((node, idx) => {
              const isEnergized = idx < currentNodeIndex;
              const isCurrent = idx === currentNodeIndex && !completed;
              return (
                <div
                  key={node.id}
                  className={`p-3 rounded-xl border text-center transition-all ${
                    isEnergized
                      ? 'bg-cyan-950/30 border-cyan-500/60 shadow-[0_0_12px_rgba(0,245,255,0.15)]'
                      : isCurrent
                      ? 'bg-slate-900 border-amber-500/60 shadow-[0_0_12px_rgba(255,184,0,0.15)]'
                      : 'bg-slate-900/40 border-slate-800/60 opacity-50'
                  }`}
                >
                  <div className="text-[9px] font-mono text-slate-400 uppercase tracking-tight">{node.terminalLabel}</div>
                  <div className={`text-xs font-bold mt-1 ${isEnergized ? 'text-cyan-300' : isCurrent ? 'text-amber-300' : 'text-slate-400'}`}>
                    {node.stageName}
                  </div>
                  <div className="mt-2 text-[10px] font-mono">
                    {isEnergized ? '🟢 CONDUZINDO' : isCurrent ? '🟡 AGUARDANDO POLO' : '⚪ DESCONECTADO'}
                  </div>
                </div>
              );
            })}
          </div>

          {/* Fichas de Conexão Causal (Opções para o nó atual) */}
          {!completed && !isShortCircuited && nodes[currentNodeIndex] && (
            <div className="space-y-3 p-4 rounded-xl bg-slate-900/60 border border-slate-800">
              <span className="text-xs font-mono text-cyan-400 tracking-wider">
                SELECIONE O CONECTOR VÁLIDO PARA FECHAR O CIRCUITO:
              </span>
              <div className="grid grid-cols-1 gap-2.5">
                {nodes[currentNodeIndex].options.map((option, oIdx) => (
                  <button
                    key={oIdx}
                    onClick={() => handleConnect(option, nodes[currentNodeIndex])}
                    className="p-3 text-left rounded-lg bg-slate-950/70 hover:bg-cyan-950/30 border border-slate-800 hover:border-cyan-500/50 text-xs text-slate-300 hover:text-cyan-200 transition-all font-mono leading-relaxed"
                  >
                    🔌 {option}
                  </button>
                ))}
              </div>
            </div>
          )}
        </div>
      )}
    </section>
  );
};
Markdown[COMANDO NATIVO]: /goal
[OBJETIVO]: Implementar o Reator Causal de Alta Tensão na Seção 03 (Motor Estrutural) com preservação total da visualização canônica e inclusão da camada interativa elétrica com Web Audio API nativo.
[ARQUIVOS EM FOCO]:
- src/lib/circuit-engine.ts
- src/lib/audio.ts
- src/index.css
- src/components/study/StructureSection.tsx
[CÓDIGO / IMPLEMENTAÇÃO]:
- Criar `src/lib/circuit-engine.ts` com o gerador determinístico `buildCircuitNodes` mapeando `causalChain` e `proportionality` em nós de 5 estágios com distratores de polaridade lógica.
- Estender `src/lib/audio.ts` adicionando `playCircuitConnectSound`, `playCircuitShortSound` e `playReactorOverdriveSound` sintetizados via AudioContext nativo sem dependências externas.
- Adicionar no `src/index.css` as keyframes elétricas `@keyframes circuitPulse` e `@keyframes breakerSpark`.
- Atualizar `src/components/study/StructureSection.tsx` inserindo o comutador `[ 📋 Matriz Canônica ]` ⇄ `[ ⚡ Reator Causal (+150 XP) ]`, mantendo o modo canônico original intacto por padrão e acionando o medidor de voltagem e o disjuntor com premiação de XP no modo Reator.
[VALIDAÇÃO VISUAL E FUNCIONAL]:
- Executar `npx tsc --noEmit` para validação de tipos TypeScript estrita.
- Abrir a Seção 03 e verificar se a Matriz Canônica inicial permanece idêntica à original.
- Comutar para o Reator Causal, simular uma conexão incorreta para verificar o disparo do disjuntor e do som de faísca, rearmar o disjuntor e completar os 5 terminais até a sobrecarga de 100 KV (+150 XP).
